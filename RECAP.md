# Helpan AI Rail — Build Progress and Sprint Tracker

**Document type:** Rail-specific progress tracker. Update at each sprint close.
**Date:** 7 May 2026 · **Last update:** 22 May 2026 (H-17 closure — Kafka outbox; cross-rail event durability)
**Cross-rail source of truth:** `c:\Projects\Platform Rails-instruction pack v1-reboot pack v1.2\Platform_Rails_Reboot_Pack_v1_2.md` (v1.2 + Amendment §A, 7 May 2026). For Helpan-AI-facing wiring, read §16.8 (cross-rail wiring — JWT TTL table, Kafka topology, `actor`/`initiated_by` propagation) and Amendment §A (especially §A.2, §A.5, §A.9, §A.11) before starting any sprint. Programme `RECAP.md` in the same folder remains the dependency-graph / critical-path view.
**Rail design corpus:** every `helpan-ai-*.md` and `helpan-ai-openapi-v1.yaml` in this folder. Reading order in `helpan-ai-reading-orders-v1.md`.

---

## 1. Where this rail is

**Design phase:** ✅ complete (all 16 Output Plan items + scan integration).

**Code:** 🟢 H-1 + H-2 + H-3 + H-3.1 + H-3b + **H-4** + H-5 + H-6 + H-7 + **H-8** + **H-8c** + **H-9** + **H-10** + **H-11** + **H-12** + **H-15** + **H-16** + **H-17** closed. **All per-app sprints closed.** Key landings (21 May 2026):
  - **H-4 — agent action dispatch.** `POST /v1/actions/dispatch` + `GET /v1/actions[/:id]`. §A.11 cross-rail audit invariant enforced on every call (two chained audit entries per dispatch — `action.dispatch` and `action.complete`/`action.fail` — both carrying `agent_id` / `delegated_authority_jti` / `target_rail` / `target_operation` / `business_op_id` / `traceparent`).
  - **H-9..H-12 — per-app block.** Six type-aware matchers wired into a strategy router by `intent.domain`: Klokd (shift_search, geo+time+pay) · Lunch Drop (weekly_plan) · Chapaa (round_up_offer with math, goal_acceleration) · family-discovery (fresh_arrivals, basket_auto_refill). Closes §6.6. Five named agents admitted with per-app safety policies; family-discovery is the only `family_friendly` audience in the portfolio.
  - **H-15 — audit-chain hardening (§6.18 close).** `audit_log.hash_version` discriminator + v2 composition covering every persisted column (eleven previously unhashed). Pre-H-15 rows verify under v1 forever; new rows are v2. `npm run audit:verify` walks the chain — live state confirmed intact. Stage 2 pen-test prep.
- **Live surfaces (24 endpoints):** health ×2 · briefings ×5 · `POST /v1/events/ingest` · oauth-scopes ×2 · operator-agents ×3 · operator-safety-policies ×2 · `GET /v1/operator/audit` · **authorities ×5** — `POST /v1/authorities` (issue/grant) · `GET /v1/authorities` (list) · `GET /v1/authorities/:id` · `POST /v1/authorities/:id/validate` (relying-party per-call check) · `POST /v1/authorities/:id/revoke` · **actions ×3** — `POST /v1/actions/dispatch` (agent action via HMAC + X-Delegated-Authority) · `GET /v1/actions` (dual-auth — Console Activity tab + operator) · `GET /v1/actions/:id`.
- **Workers (5 Railway processes — Procfile `web`/`worker`/`cascade`/`reaper`/`outbox`):** `webhookDelivery` drains `webhook_deliveries` (`FOR UPDATE SKIP LOCKED`, HMAC-signed POST, 30s→24h retry × 8); `cascadeRevocation` (H-3b) consumes `identiti.account.events` and auto-revokes authorities on `ACCOUNT_SUSPENDED` (all) / `TIER_CHANGED` downgrade (high-stakes only); `actionReaper` (H-16) settles orphan pending actions older than 10 min; `kafkaOutbox` (H-17) drains `kafka_outbox` and publishes to Kafka — producers enqueue inside the business tx so emission is atomic with the write (closes RECAP §6.7).
- **Auth:** HMAC (rail-wide default) + Identiti customer-JWT. Customer-JWT covers `/v1/briefings/*` (mandatory) and — since **H-8a** — the Console authority surface `/v1/authorities` GET/POST + `/:id` GET + `/:id/revoke` (dual-auth: customer-JWT *or* HMAC; a customer caller is hard-scoped to its own `account_uuid`). `/v1/authorities/:id/validate` stays HMAC-only. The shared `authPlugin` stands down when an earlier preHandler set `request.appId`; the customer-JWT plugin is registered before it. Admin surfaces require `helpan:admin`; authority service endpoints require per-endpoint scopes — `requireScope(request, scope)`.
- **DB:** migrations 0001–**0016** apply cleanly. Pointed at Supabase project `vjaebospxjisiiuxafgo` (eu-west-1, session pooler port 5432) — replaces `jvkhoveeayixbjnhmqxa` which became unreachable (paused) on 22 May 2026. All migrations re-applied; all 5 `app_credentials` re-seeded with fresh HMAC secrets; all 5 agents re-admitted. Audit chain: 6 rows (genesis + 5 v2-hashed admissions) — `v2 rows: 5`, CHAIN INTACT. 0010 admits `itafika`/`hakken`/`kipkiren_web_services` rails + 7 paper scopes. 0011 (H-9) adds 3 klokd rows. 0012 (H-10) adds 2 lunchdrop rows. 0013 (H-11) adds 4 chapaa rows (incl. the `read_behavioural` friction-screen scope). 0014 (H-12) adds 2 family_discovery rows. 0015 (H-15) adds `audit_log.hash_version smallint CHECK (1, 2)` for the full-column hash hardening. **All 5 `app_credentials` rows seeded** (22 May 2026) — `lunchdrop` (admin + consuming-app scopes; bootstrap admin tenant), `klokd` / `chapaa` / `family_discovery` (each with the consuming-app scope set: authorities issue/validate/revoke + actions dispatch/read), `helpan_ai` (internal/operator: admin + operator:read). Plaintext HMAC secrets handed off out-of-band; the noop envelope wraps each at rest. Re-running `npm run db:seed:app` rotates the secret on conflict. 0009 added `business_op_id` to `audit_log` + `actions` (§A.11). RLS enforced on `briefings` (FORCE) and `audit_log` (operator-only SELECT).
- **Agent registry:** five rows. `helpan-kws-v1` (Phase-1 paper-only — H-8c). `helpan-klokd-v1` / `helpan-lunchdrop-v1` / `helpan-chapaa-v1` (v1.0 active, H-9 / H-10 / H-11). `helpan-family-discovery-v1` (v1.0 active — H-12; **`audience_posture=family_friendly`** — the only one in the portfolio; category whitelist food/household/baby/school/basic-clothing; blacklist nightlife/alcohol/adult; `location_precision_floor=merchant_level`; user-to-user agent comms disabled via metadata sentinel per §4.6; brand-name TBD per §4.8). All stable-named per memory `project-stable-agent-ids`; admission entries on the audit chain with §A.11 `agent_id` populated.
- **Audit chain:** writer (`src/lib/auditWriter.ts`) extended at H-3.1 with the §A.11 indexed fields — `agentId`, `delegatedAuthorityJti`, `targetRail`, `targetOperation`, `businessOpId`. H-15 (21 May 2026) adds `hash_version` to `audit_log` and bumps the writer to v2: every persisted column (actor_type / account_uuid / app_id / outcome / initiated_by / traceparent / agent_id / delegated_authority_jti / target_rail / target_operation / business_op_id / resource_type / request_id) is now folded into `entry_hash`. Pre-H-15 rows (incl. genesis + the 5 admission entries) stay at v1 and verify under the legacy composition; new rows after H-15 are v2. Verifier at `npm run audit:verify` walks the chain row-by-row, dispatching by per-row hash_version. Closes RECAP §6.18.
- **Delegated authorities (H-3):** Helpan AI authors the §2.3 claim set; Identiti signs via `POST /v1/internal/sign` (Helpan AI never holds the DA key). Validation verifies the RS256 signature vs Identiti JWKS + DB status/limits — pure query, HTTP 200 even on `valid=false`. Revocation immediate + idempotent. `helpan.authority.events` publishes `AUTHORITY_ISSUED` / `AUTHORITY_REVOKED`.
- **Kafka:** producer surface in `src/lib/kafka/{topics,producer}.ts` — `kafkajs` prod / in-memory test stub. Cascade worker adds the first *consumer* (`identiti.account.events`, group `helpan.identiti.account.events.consumer`). Producer/consumer optional — missing brokers → publishes skipped, DB + webhook paths still work; the cascade worker exits 1 without brokers.
- **Helpan Console (H-8b):** separate RN library codebase at `C:\Projects\helpan-console\` (`@kmv/helpan-console`). **Foundation shipped** — types, `ConsoleApiClient`, EN/SW i18n, theme tokens, formatters, pure `consoleReducer`; 43/43 tests, typecheck clean, no RN dependency. **UI layer (components + screens + `HelpanConsole.open()` + Storybook) deferred** — tracked as §6.19; reactivates when an embedding app schedules it. Not yet a git repo — needs a `helpan-console` GitHub repo before the UI layer resumes.
- **Tests:** rail **294/294 green** post-H-16. Console foundation **43/43**. Typecheck + lint clean across both. Cumulative growth — H-4 added 33; H-9 added 20; H-10 added 11; H-11 added 15; H-12 added 15; H-15 added 21; H-16 added 6 (reaper real-DB integration: settle happy path, fresh actions untouched, empty pool, batchSize honoured, audit chain stays linked, age_seconds detail). Real-DB tests timeout-bumped to 30s in `vitest.config.ts` to absorb the 4-transactions-per-test cost.
- **Deploy:** `@kmv/platform-shared` vendored to `vendor/platform-shared/`; rail at GitHub `whyyam1/helpan_ai` (HEAD `dcffce4`, 13 commits, `Co-Authored-By: Claude` trailer stripped from history 19 May 2026 + force-pushed). Railway service `web` **live as of 20 May 2026** on Railpack v0.23.0 / Node 22.22.3 — container boots cleanly after the 12 service variables were added to Railway's Raw Editor (initial crash loop was `Missing required environment variable: AUTH_RAIL_PREFIX`; `.env` is gitignored so nothing reached Railway until the paste). `web` only; `worker` (webhookDelivery) + `cascade` (cascadeRevocation) processes from the Procfile not yet provisioned.
- **Next:** All four per-app sprints + the §6.18 audit-hash hardening closed. The only buildable rail-side sprints remaining are gated:
  - **H-13** (Console UX validation 10+ users) — waits on §6.19 Console UI layer being built.
  - **H-14** (cross-rail end-to-end testing) — waits on all other rails reaching Sprint 5+.
  - **H-8d** (Helpan KWS Phase-1 enrichment runner) — waits on KWS Sprint 6+.
  - **Hakken consumer wiring** — waits on HK-3.
  - **Identiti consent-cascade extension** — waits on ID-14.
  
  Buildable without external unblocks: (i) §6.4 RLS write policies on `delegated_authorities` under a system role (Stage 2 hardening); (ii) §6.7 Kafka outbox table for cross-rail event durability; (iii) §6.19 Console UI layer (substantial; unblocks H-13). Brand-lock for `helpan-family-discovery-v1` required before Stage 2 per §4.8. (app_credentials seed and pending-action reaper closed 22 May 2026.)
  
  **Production wire-up of H-4:** set `HELPAN_KP_URL` / `HELPAN_KP_HMAC_SECRET` (and matching Todoku/Identiti pairs) in Railway when those rails publish staging URLs — until then the HTTP dispatcher resolves to `createUnconfiguredDispatcher` and actions persist as `failed` with `TARGET_RAIL_UNCONFIGURED`, which is correct deploy-safe behaviour.

**Stack:** Node.js 22 LTS · TypeScript 5.x strict · Fastify 4.x · AJV 2020-12 · PostgreSQL 16 (Supabase af-south-1 in non-dev) accessed via **`postgres` (postgres-js, `prepare:false`)** — rail-wide Drizzle driver standard, matches Todoku TD-0 · Drizzle ORM + drizzle-kit · `jose` 5.x for RS256 customer-JWT verify · `kafkajs` 2.x with `LegacyPartitioner` + `idempotent: true` + `acks: -1` · ESLint 9 flat config · Vitest · Railway. **LLM provider open** per Confirmation Memo §5.8.

**Dependencies:** `@kmv/platform-shared` from `C:\Projects\platform-shared\` via `file:..` path dep — pnpm workspace migration is open question (b) under §6.3.

**Standing position:** Helpan AI is the **agent-runtime rail** — per Reboot Pack v1.2 Amendment §A.5, **not** a fourth rail in the §3 three-rails-at-a-glance table (Pay / Identity / Comms remain the canonical regulated rails). Helpan AI orchestrates them on behalf of agents and owns the scope catalogue, agent registry, action dispatch, agent audit log, and delegated-authority registry. The split is canonical: Identiti is the OAuth issuance authority for agents (regulator-facing); Helpan AI is the per-call validation authority that KP and Todoku consult when `actor.type='agent'`. Supersedes scan item ID-2. Build sequencing unchanged: H-1 + H-2 ran in parallel with the other rails' early sprints; H-3 onward is gated by cross-rail unblocks.

**Cardinal rule:** Helpan AI does NOT duplicate platform rail functionality. No funds, no credit, no yield, no float, no off-ledger netting. No identity / KYC / credentials. No direct comms delivery.

---

## 2. Sprint plan

Conventions: 2-week sprints. Status: [ ] open · [~] in flight · [x] done.

| Sprint | Goal | Maps to | Cross-rail deps | Status | Notes |
|---|---|---|---|---|---|
| **H-1** | Foundation — Fastify scaffold, auth + idempotency middleware, `GET /v1/health` + `/v1/health/deep`, DB migrations 0001–0006 per Schema and ERD §5 | OpenAPI §health, §components | None (uses `@kmv/platform-shared` only) | [x] | Closed 8 May 2026. Typecheck clean, lint clean (ESLint 9 flat config), 16 tests pass (env validator 7 + secrets envelope 3 + health integration 6). Cross-rail patch: `@kmv/platform-shared/src/hmac.ts` accepts `'Helpan'` as a `RailPrefix` — rebuilt + 37/37 platform-shared tests pass. Driver: `postgres-js` with `prepare:false` to match Todoku TD-0. **Follow-ups (do not block close):** real-Postgres smoke (env-dependent, no Docker on Windows), three scope decisions to Chamia tracked in §6. |
| **H-2** | Briefings CRUD — `POST/GET/PATCH/DELETE /v1/briefings`, RLS policies | OpenAPI Briefings tag | Identiti ID-3 (customer JWT validation) | [x] | Closed 9 May 2026. Five briefings routes wired; customer-JWT verifier rail-local (decision 1a — promote to platform-shared once KP-2/TD-2 also need it); migration 0007 adds INSERT/UPDATE/DELETE policies + `FORCE ROW LEVEL SECURITY` (closes 0006's SELECT-only gap on `briefings` — H-3 should replicate for `delegated_authorities`, H-4 for `actions`); audit-log hash-chain writer (`src/lib/auditWriter.ts`) seeded for H-4 reuse; deep-health gains `briefings` component. Auth narrowed to BearerCustomer per decision 2a — HMAC + service-JWT on `/v1/briefings/*` deferred. New dep: `jose` ^5.9.6. 53/53 tests green. Detail in §5; open follow-ups in §6. |
| **H-3** | **Delegated authorities issuance + validation + revocation** — `POST /v1/authorities`, `POST /v1/authorities/{id}/validate` (per-call XR-1 contract — relying rails MUST validate per Reboot Pack v1.2 Amendment §A.2 when `actor.type='agent'`), `/revoke`, Kafka `helpan.authority.events` (topology MUST not preclude future CAEP per-token revocation events per Reboot Pack §A.9). JIT semantics mandatory per §A.1 (short-TTL, audience-bound, single-operation). | OpenAPI Authorities tag; Delegated Authority Contract §3, §4, §5, §8; Reboot Pack v1.2 §16.8 + §A.2 | H4 joint with Identiti ID-10 — **ID-10 closed**, `POST /v1/internal/sign` live. | [x] | Closed 18 May 2026. 5 authority endpoints (issue/list/get/validate/revoke), all HMAC + per-endpoint scope (decision 1a). Issuance: validate inputs → step-up verify (high-stakes) → build §2.3 claims → Identiti `POST /v1/internal/sign` → persist + audit + publish `AUTHORITY_ISSUED`. Validation: RS256 signature verify vs Identiti JWKS + DB status/scope/limit checks, pure query (HTTP 200 even on `valid=false`). Revocation: immediate, idempotent, publishes `AUTHORITY_REVOKED`. Cascade-revocation Kafka *consumer* deferred to fast-follow (decision 2a, §6.14). Idempotency plugin gained `exemptSuffixes` (cross-rail platform-shared change, decision 3a) so `POST /validate` is key-exempt. No new migration (§6.13). 42 new tests; 153/153 green. Detail in §5; open follow-ups in §6. |
| **H-4** | Action dispatch — `POST /v1/actions/dispatch`, target-rail routing, audit log writes with hash chain. **Cross-rail audit invariant** (Reboot Pack v1.2 §A.11): every dispatched action MUST carry shared `traceparent` + `business_op_id`, with `actor` + `initiated_by` matching the values KP/Todoku will persist on their side. Hard build-acceptance criterion. | OpenAPI Actions tag; Reboot Pack v1.2 §A.2, §A.11 | KP KP-8 + Todoku TD-1 (relying parties accept `X-Delegated-Authority`) | [x] | Closed 21 May 2026. Three live endpoints: `POST /v1/actions/dispatch` (HMAC + `X-Delegated-Authority`), `GET /v1/actions` and `GET /v1/actions/:id` (dual-auth — Console reads via customer JWT, operators via HMAC). Validation re-uses H-3 `validateAuthority`. Two-phase persistence: Phase A inserts a `pending` action + `action.dispatch` audit entry + bumps `authority_usage` (atomic); Phase B forwards to the target rail via `TargetRailDispatcher` (HTTP impl per rail + in-memory for tests + `createUnconfiguredDispatcher` sentinel when the URL is empty); Phase C updates terminal status + writes `action.complete`/`action.fail` audit entry. Every audit row carries §A.11 fields (`agent_id`, `delegated_authority_jti`, `target_rail`, `target_operation`, `business_op_id`, `traceparent`) so cross-rail forensic joins land. New Kafka topic `helpan.action.events` (ACTION_DISPATCHED / COMPLETED / FAILED) — skipped when no brokers. PII redactor strips phones/emails/PANs from the persisted payload but NOT from the outbound dispatch (relying rail needs the real value). HTTP dispatcher signs with rail-specific HMAC prefix (`Kipkiren-`/`Todoku-`/`Identiti-HMAC-SHA256`); URLs / secrets independently opt-in per rail. 33 new tests (19 unit + 14 real-DB integration). Total 206/206 green. Detail in §5; open items in §6. |
| **H-5** | Matching engine — `POST /v1/events/ingest`, briefing-to-event matching, `BRIEFING_MATCHED` Kafka event + webhook fan-out | OpenAPI Events tag | None | [x] | Closed 11 May 2026. HMAC-authed `/v1/events/ingest`; synchronous matching engine ships the **generic key-equality matcher** per decision 2b (type-aware matchers deferred to per-app sprints H-9..H-12); Kafka producer surface (`kafkajs` prod / in-memory test); webhook delivery worker as separate Railway entrypoint (`npm run worker:webhooks`) — HMAC-signed POST, `FOR UPDATE SKIP LOCKED` claim, retry 30s/1m/5m/15m/1h/4h/12h → abandoned. Migration 0008 added `webhook_deliveries.target_url`. Audit-log writer (from H-2) appends one `event.ingested` entry per ingest with `matched_briefings[]` detail. New dep: `kafkajs` ^2.2.4 (was pinned but unused). 79/79 tests green. Detail in §5; open follow-ups in §6. |
| **H-6** | OAuth scope catalogue + agent registry endpoints, per-app safety policies | OpenAPI OAuth + Operator tags | None | [x] | Closed 11 May 2026. Three sub-surfaces wired: OAuth scopes (public GET + admin POST), Operator agents (POST register / GET / PATCH status), Operator safety policies (GET list / PUT upsert). Rail-local `requireAdminScope` helper checks `helpan:admin` on the HMAC tenant — promotion to platform-shared once KP/Todoku need scoped admin endpoints. Safety policy PUT is RFC-compliant upsert with explicit 409s on `app_id` frozen and duplicate-policy-per-app. All write paths audit-logged. v1.0 narrowing: GET /v1/oauth/scopes is HMAC-authed (OpenAPI says public) — see §6.10. `vitest.config.ts` gains `fileParallelism: false` to fix real-DB test races. 104/104 tests green. Detail in §5; open follow-ups in §6. |
| **H-7** | Operator console — agent registration, safety policy admin, audit log query | OpenAPI Operator tag | None | [x] | Closed 11 May 2026. Narrowed to `GET /v1/operator/audit` since agent + safety endpoints shipped at H-6. Filters: account_uuid · agent_id · action · from · to. Paginated by base64url cursor (`<created_at>|<id>`), same shape as briefings. Query runs in a tx that sets `app.role='operator'` so migration 0006's operator-only RLS policy on `audit_log` is satisfied. Hash-chain fields (`previous_hash`, `entry_hash`) included in the response for tamper-evident reads. 111/111 tests green (7 new integration tests). |
| **H-8** | **`@kmv/helpan-console` React Native shared library** — Console specification §3 IA, §4 behaviour, §6 cross-rail. Separate codebase but parallel sprint | Helpan Console Specification | H-3 (authorities API live) | [x] | Closed 21 May 2026 (scope-trimmed). **H-8a** (rail-side, commit `9256e6b`): dual-auth on the Console authority surface — `/v1/authorities` issue/list/detail/revoke accept the Identiti customer JWT *or* HMAC; customer caller hard-scoped to its own account; `/validate` stays HMAC-only; 10 new tests, rail 173/173. **H-8b** (RN library): foundation shipped at `C:\Projects\helpan-console\` — types, API client, EN/SW i18n, theme, formatters, pure reducer; 43/43 tests; typecheck clean; no RN dependency. **UI layer (components + screens + `HelpanConsole.open()` entry + Storybook) deferred to a future Console sprint — see §6.19**; reactivates when an embedding app (Lunch Drop / Chapaa / Klokd) schedules it and Activity-tab dep (H-4) lands. |
| **H-8c** | **Admit `helpan-kws-v1` to rail-side agent registry + newdocs scope catalogue additions** — closes McKinsey P1 + Reboot Pack §14 open CEO action per [NEWDOCS_INSTRUCTION_PACK.md](NEWDOCS_INSTRUCTION_PACK.md) §H-8c + §H-NN | Helpan KWS instruction pack v1; Itafika S0; Hakken §9.4 | None (data-only) | [x] | Closed 21 May 2026. Migration 0010 extends `oauth_scopes_rail_chk` to admit `itafika` / `hakken` / `kipkiren_web_services` and seeds 7 catalogue rows (`delivery.dispatch`, `discovery.query`, `kws.{dns,ssl,mx,domain,uptime}.write` — paper-only). New seed script `scripts/seedHelpanKwsAdmission.ts` registers `helpan-kws-v1` (portfolio_app, owner=`kipkiren_web_services`) with a Phase-1-safe safety policy (blacklists `write_money`) and chains an `agent.register` audit entry with the §A.11 `agent_id` indexed column populated. **Open questions deferred:** Q3 (`delivery.dispatch` rail class), Q4 (Phase-2 execution-site decision), Q5 (KWS AI decomposition runtime), Q7 (KWS TTL — pack default 72h vs §A.1 JIT norm ≤15min — this migration takes the JIT-compliant side at 3600s; revisit when Identiti + Helpan rule jointly). Rail tests still 79/79 always-on green (94 real-DB skipped without `TEST_DATABASE_URL`); no regression. |
| **H-9** | Per-app: **Helpan Klokd** (priority 1) — pay-on-completion via dispatch, reputation surfacing | Per-App Integration Patterns §1 | KP KP-8/9 + Todoku TD-9 | [x] | Closed 21 May 2026. Migration 0011 adds three klokd-namespace scopes (`klokd.write.shift_pay` write_money 200k/5M; `klokd.write.shift_signup` admin/count-limited; `klokd.read.worker_reputation` read_aggregate). New `src/lib/matching/klokdShiftMatcher.ts` — pure type-aware matcher: haversine geo (`max_distance_km` + `origin`), category whitelist, tz-aware time window (cross-midnight handled via `Intl.DateTimeFormat`), pay floor. `src/lib/matching/engine.ts` refactored to a strategy router — dispatches on `briefing.intent.domain`, falls back to the H-5 generic key-equality matcher when unset/unknown — backward-compatible with every existing matcher test. `helpan-klokd-v1` agent + §1.6 safety policy admitted via `scripts/seedHelpanKlokdAdmission.ts` + `npm run db:seed:helpan-klokd` (audience_posture=general, location_precision_floor=merchant_level). **Closes RECAP §6.6** (type-aware matchers per briefing_type deferred to per-app sprints). 20 new tests (17 matcher unit + 3 engine routing). Pay-on-completion / reputation-surfacing endpoints themselves live on the embedding Klokd backend; the rail's contribution is the catalogue + agent + matcher. Detail in §5; pay-on-completion live flow in §1.5 of the Per-App Patterns spec. |
| **H-10** | Per-app: **Helpan Lunch Drop** (priority 2) — ZoneFeed augmentation, weekly plans, reliability nudges | Per-App Integration Patterns §2 | KP KP-8 + Todoku TD-9 | [x] | Closed 21 May 2026. Migration 0012 adds two lunchdrop-namespace scopes (`lunchdrop.write.orders` write_money 100k/3M weekly, `lunchdrop.read.zone_feed` read_aggregate). New `src/lib/matching/lunchDropPlanMatcher.ts` — type-aware matcher for `lunchdrop.weekly_plan` briefings: primary-or-fallback merchant_id check, case-insensitive menu_preference hit (at least one item must match), per-order ceiling enforcement. Registered in the engine's `DOMAIN_MATCHERS` table — one-line addition per the H-9 strategy refactor. `helpan-lunchdrop-v1` agent + §2.6 safety policy admitted via `scripts/seedHelpanLunchDropAdmission.ts` + `npm run db:seed:helpan-lunchdrop`. 11 new tests (10 matcher unit + 1 engine routing). Cron-driven scheduled firing for weekly_plan is v1.1 — depends on KP programmable-money (§2.7 C.3). |
| **H-11** | Per-app: **Helpan Chapaa** (priority 3, highest stakes) — goal nudges, round-up, MMF suggest-only, credit unlock surface | Per-App Integration Patterns §3 | KP KP-8/13 (S.1 partner-lender) | [x] | Closed 21 May 2026. Migration 0013 adds four chapaa-namespace scopes — `chapaa.write.deposit` (round-up auto-accept, KES 300/call · 10k/month), `chapaa.read.behavioural` (the friction-screen scope — `read_behavioural` category, `default_grantable=false`), `chapaa.read.credit_unlock_status`, `chapaa.read.goals`. New `src/lib/matching/chapaaMatchers.ts` with **two** matchers: `chapaa.round_up_offer` (pure math — listens to KP `WALLET_DEBITED`, computes `round_up_minor` against configurable unit, default 100 KES, exposes `computed_round_up_minor` in match detail for downstream dispatch) and `chapaa.goal_acceleration` (`threshold_watch` briefing — matches by `goal_id` + `alert_when` signal). Both registered in `DOMAIN_MATCHERS`. `helpan-chapaa-v1` agent + §3.6 safety policy admitted via `npm run db:seed:helpan-chapaa`. 15 new tests (13 matcher unit + 2 engine routing). MMF rebalance stays **suggest-only** at v1.0 per §3.6 — no `chapaa.mmf.rebalance` scope; v1.1 candidate. Credit-unlock partner-lender architecture (§3.7) is KP-side; rail surfaces only via `chapaa.read.credit_unlock_status`. |
| **H-12** | Per-app: **Helpan [App Name]** (family-discovery, priority 4) — discovery briefings, standing-basket | Per-App Integration Patterns §4 | KP KP-8/9/10 (all three) + Todoku TD-9 | [x] | Closed 21 May 2026. Last per-app sprint. Migration 0014 adds two family_discovery-namespace scopes (`family_discovery.write.basket` write_money 250k/2M monthly, `family_discovery.read.discovery` read_aggregate). New `src/lib/matching/familyDiscoveryMatchers.ts` — two matchers in one module: `family_discovery.fresh_arrivals` (alert; haversine geo + category whitelist + tz-aware time-window + max-price ceiling; re-uses the Klokd matcher's pure helpers `haversineKm`/`minutesInWindow`/`minutesOfDayInZone`/`parseHhmm` to avoid duplicating tz/geo math) and `family_discovery.basket_auto_refill` (standing_basket; merchant-id allowlist + per-SKU price-ceiling — rejects on any unrecognised SKU per family-friendly safety intent — + basket total ceiling). Both registered in `DOMAIN_MATCHERS`. `helpan-family-discovery-v1` agent + the only `family_friendly` audience_posture in the portfolio admitted via `npm run db:seed:helpan-family-discovery`. 15 new tests (13 matcher unit + 2 engine routing). Cron-driven standing-basket firing is v1.1 — depends on KP C.3 programmable money (§4.7 REQUIRED, no v1.0 workaround); the matcher is plumbed so the v1.1 unlock is plug-and-play. Brand name TBD per §4.8 — search/replace `[App Name]` and `family_discovery` at brand-lock time, before Stage 2. |
| **H-17** | Kafka outbox (§6.7 close) — `kafka_outbox` table; producers enqueue inside the business tx so emission is atomic with the write; `kafkaOutbox` worker drains via `FOR UPDATE SKIP LOCKED` and publishes to Kafka. Closes the v1.0 cross-rail event-durability gap. | RECAP §6.7; ERD addition | None — pure hardening, no external deps | [x] | Closed 22 May 2026. Migration 0016. New `src/lib/kafka/outbox.ts` (enqueue/claim/markDelivered/markFailed/drainOutboxOnce). 5 producer call sites migrated (events/authorities/actions/cascade — all now write to outbox inside tx, no more after-commit publish). New `src/workers/kafkaOutbox/index.ts` worker (5th Procfile process). `drainTestOutbox` test helper. 7 new integration tests; 5 existing tests updated to drain before assertion. 301/301 green locally. Supabase migration application pending one transient pooler ENOTFOUND retry. |
| **H-16** | Pending-action reaper (§6.21 close) — fourth Procfile process (`reaper`) that polls `actions.status='pending'` and settles rows older than `REAPER_STALE_AFTER_SECONDS` to `failed/REAPER_UNRESOLVED`. Closes the H-4 Phase-A-commit-Phase-B-crash window where an action sat in `pending` indefinitely. | RECAP §6.21; H-4 dispatch flow | None — pure hardening, no external deps | [x] | Closed 22 May 2026. No new migration. `src/modules/actions/repo.ts` gains `listStalePendingActions` using `FOR UPDATE SKIP LOCKED` (multi-replica safe). `src/workers/actionReaper/` — pure `reapStaleActions(deps, options)` (per-row settle in one transaction so row UPDATE + audit-fail entry commit atomically) + Railway entrypoint with SIGTERM-aware loop. `Procfile` gains `reaper`. Three new env vars (poll interval / staleness / batch size) with sensible defaults; all optional. Settlement is deliberate — does NOT retry (Phase B's original payload is lost on crash; the redacted payload would corrupt downstream state if forwarded). The honest settlement is `failed/REAPER_UNRESOLVED`; consuming app retries under a fresh idempotency_key if it wants. `action.fail` audit entry written with `actor_type='system'`, `actor_id='helpan-ai-rail:reaper'`, §A.11 columns copied from the pending row. 6 new real-DB integration tests. |
| **H-15** | Audit-chain full-column hash hardening (§6.18 close) — `hash_version` discriminator + v2 composition covering every persisted column. Stage-2 pen-test prep. | RECAP §6.18; ERD §1.13; Reboot Pack §9.5 | None — pure hardening, no external deps | [x] | Closed 21 May 2026. Migration 0015 adds `audit_log.hash_version` (NOT NULL, CHECK IN (1,2), default 1 for backfill). Writer (`auditWriter.ts`) gains `computeEntryHashV2` covering eleven previously-unhashed columns + a `"v2"` discriminator prefix in the hash, plus `computeEntryHashForVersion(v, ...)` dispatcher used by the verifier. `appendAuditEntry` pinned to `CURRENT_AUDIT_HASH_VERSION=2`. v1 composition preserved verbatim — pre-H-15 rows (incl. genesis + 5 admission entries) verify forever. New verifier `scripts/verifyAuditChain.ts` (+ `npm run audit:verify`) walks the chain row-by-row, recomputes per-row hash by its declared version, reports drift. Live verification on Supabase confirmed CHAIN INTACT (6 rows: 5 v1 + 1 genesis). 21 new unit tests (v1 snapshot regression-guard with a locked golden hash, v2 determinism + key-order invariance, parameterised tamper-detection across all eleven new columns, v1/v2 dispatcher). |
| **H-13** | UX validation 10+ users (DoD §13.2 S11), Console hardening | DoD S11 | H-8 (Console live) | [ ] | |
| **H-14** | Cross-rail end-to-end testing | Handoff §14 | All rails Sprint 5+ | [ ] | |
| **H-Beta** | Stage 2 closed beta — 50+ users per app cohort | DoD §7.3 | H-13 + per-app sprints | [ ] | |
| **H-GA** | Stage 3 GA — family-discovery + Chapaa live; Lunch Drop / Klokd live or formally deferred | DoD §7.4 + §10 | H14 closed; pen-test C+H | [ ] | |

**Stage 1 sandbox target:** end of H-7 (~8 weeks; depends on Identiti ID-10 and KP KP-8 unlocking by then).
**v1.0 launch criterion:** family-discovery + Helpan Chapaa live and passing DoD Stage 3.

---

## 3. What changes between sprints

After each sprint:

1. Update row: `[ ]` → `[x]`. Notes for scope shifts.
2. Cross-update central tracker: `RECAP.md` §4.4.
3. Cross-update Helpan AI Build Readiness Checklist (`helpan-ai-build-readiness-checklist-v1.md`) — flip the corresponding row.
4. If H-3 lands → notify KP and Todoku to begin/continue their delegated-authority validator integrations.

After **Stage advances**:

1. Update central tracker §1.
2. Re-issue Helpan AI Reboot Pack at the next minor version with updated state.
3. Sign-off per DoD §14.

---

## 4. Source-of-truth pointers

1. `helpan-ai-reboot-pack-v1.md` — canonical record for this rail
2. `helpan-ai-design-reference-v1.md` + Amendment §A — the "why"
3. `helpan-ai-openapi-v1.yaml` — wire-level spec
4. `helpan-ai-delegated-authority-contract-v1.md` — most security-critical
5. `helpan-ai-schema-erd-v1.md` — DB design
6. `helpan-ai-event-bus-contract-v1.md` — Kafka topology
7. `helpan-ai-threat-model-v1.md` — threats and mitigations
8. `helpan-ai-oauth-scope-catalogue-v1.md` — scopes
9. `helpan-ai-per-app-integration-patterns-v1.md` — Klokd, Lunch Drop, Chapaa, family-discovery
10. `helpan-ai-console-specification-v1.md` — Console UX
11. `helpan-ai-build-readiness-checklist-v1.md` — execution roadmap
12. `helpan-ai-reading-orders-v1.md` — who reads what
13. `helpan-ai-scan-integration-memo-v1.md` — what changed because of the scan
14. Cross-rail: `Platform_Rails_Reboot_Pack_v1_2.md` (v1.2 + Amendment §A, 7 May 2026 — §16.8 cross-rail wiring + §A.2 actor/initiated_by propagation + §A.5 Helpan-AI-as-agent-runtime-rail + §A.9 roadmap + §A.11 audit invariant), all three Rail Contracts + Amendments §A, App Integration Guide + Amendment §A, Claude Code Instruction Pack + Amendment §A

---

## 5. Sprint artefacts inventory (update as you go)

| Sprint | Artefacts produced |
|---|---|
| H-1 | Fastify scaffold + tsconfig (strict) + drizzle-kit + ESLint 9 flat config + vitest config; 14 Drizzle ORM schema files (`src/db/schema/*`); 6 hand-authored SQL migrations matching ERD §5 (`0001_universal_tables` … `0006_rls_policies`) + `meta/_journal.json` + migrations README; rail-local Fastify plugins (`db`, `credentialStore`, `idempotencyStore`, `requestId`, `errorMapper`); shared HMAC auth + idempotency plugins wired with `/v1/health` exempt; `health` module (`/v1/health` + `/v1/health/deep`); env validator (`src/config/env.ts`) + unit tests; placeholder noop `secretsEnvelope` lib + unit tests; migration runner (`scripts/migrate.ts`); integration test (`test/integration/health.integration.test.ts`); cross-rail patch to `@kmv/platform-shared/src/hmac.ts` adding `'Helpan'` to `RailPrefix` and `AUTH_HEADER_RE`. |
| H-2 | New rail-local Fastify plugins: `customerJwtPlugin` (RS256 verify via `jose`, `aud`/`iss`/`exp` checks, decorates `request.customerJwt`+`request.appId` from `X-App-Id`), `rlsContextPlugin` (`withCustomerContext` runner — opens Drizzle transaction + `set_config(app.account_uuid, …, true)` + `app.role='user'`); new `src/lib/auditWriter.ts` with `appendAuditEntry(tx, …)` + pure `computeEntryHash(...)` + `pg_advisory_xact_lock` serialisation; new `src/modules/briefings/` (schemas, repo, service, routes for `POST/GET/LIST/PATCH/DELETE /v1/briefings`); migration `0007_briefings_rls_writes.sql` (INSERT/UPDATE/DELETE policies + `FORCE ROW LEVEL SECURITY`); env config extended (`IDENTITI_JWKS_URL`, `IDENTITI_JWT_ISSUER`, `HELPAN_JWT_AUDIENCE`); deep-health gains `briefings` component; test helpers `testJwks.ts` + `testDb.ts` (real-Postgres harness skips when `TEST_DATABASE_URL` unset); seven test files totalling 41 stub-DB + 12 real-DB checks. New dep: `jose@^5.9.6`. |
| H-5 | New `src/lib/kafka/{topics,producer}.ts` — topic catalogue + `KafkaProducerLike` interface with `kafkajs` and in-memory implementations; new `src/plugins/kafkaPlugin.ts` (optional registration); new `src/lib/matching/{engine,engine.test}.ts` — pure generic key-equality matcher with 10 unit tests; new `src/modules/events/` (schemas, repo, service, routes for `POST /v1/events/ingest` — synchronous match → enqueue webhook → publish-after-commit); new worker package `src/workers/webhookDelivery/` (pure `processBatch` + Railway entrypoint with SIGTERM-aware drain, HMAC-signed POST, retry schedule, `FOR UPDATE SKIP LOCKED`); migration `0008_webhook_target_url.sql`; deep-health gains `events_ingest` + `kafka` components; env extended (`KAFKA_BROKERS`, `KAFKA_CLIENT_ID`, `WEBHOOK_HMAC_SECRET`, `WEBHOOK_DELIVERY_POLL_MS/BATCH_SIZE`, `HELPAN_WEBHOOK_URL_<APP>`); test helpers gain `hmacHeaders()` + `makeWebhookTargetResolver()` + Kafka producer injection + advisory-locked migrate to fix Vitest worker race; two new integration test files (`events.ingest` + `webhookWorker`) totalling 11 real-DB + 12 stub/unit checks. New dep: `kafkajs@^2.2.4` (previously pinned but unused). |
| H-6 | New rail-local `src/lib/scopeCheck.ts` — `requireAdminScope(request)` + `AdminScopeRequiredError` (403 `AUTH_SCOPE_REQUIRED`); three new modules: `src/modules/oauthScopes/` (schemas, repo, service, routes — public GET list + admin POST create), `src/modules/operatorAgents/` (POST register with class-invariant checks for `portfolio_app` / `third_party_oauth`, GET detail, PATCH status with `suspended_at` / `retired_at` stamps), `src/modules/operatorSafetyPolicies/` (GET list, PUT upsert with `app_id` frozen + UNIQUE(app_id) enforcement). All admin writes audit-logged. Test helper `hmacHeaders(...)` extended with `appId` / `hmacSecret` overrides; `makeTestCredentialStore` now serves two tenants (admin + non-admin) for scope-rejection tests; `resetTestData` extended for `agents` / `safety_policies` / `oauth_scopes WHERE id LIKE 'test.%'`; `vitest.config.ts` gains `fileParallelism: false`. Three new integration test files (oauthScopes 7 + operatorAgents 7 + operatorSafetyPolicies 7 = 21 real-DB checks) + scopeCheck unit (4). 25 new tests; total 104/104. No new deps. |
| H-7 | New `src/modules/operatorAudit/` (schemas, repo, service, routes) for `GET /v1/operator/audit`. Read-only; admin-scope gated. Filters by account_uuid · agent_id · action · from · to. Pagination via base64url cursor (same `<iso_created_at>\|<id>` shape as briefings). Repo opens a tx and `set_config('app.role', 'operator', true)` so migration 0006's operator-only SELECT policy on `audit_log` admits the query. New integration test file (`operatorAudit.integration.test.ts`) with 7 real-DB checks covering happy path · filter-by-action · filter-by-account · cursor pagination · cursor-invalid → 400 · non-admin rejection · `limit=0` → 400. 7 new tests; total 111/111. No new deps. |
| H-3 | New `src/lib/identitiSigner.ts` (`DelegatedAuthoritySigner` interface + `createHttpIdentitiSigner` — HMAC-signed client for Identiti `POST /v1/internal/sign`), `src/lib/stepUpVerifier.ts` (RS256 + `aud=helpan_authority_issuance` check on the issuance-time step-up token); new `src/modules/authorities/` — `scopeClassifier.ts` (pure: TTL ceiling + high-stakes derive over an `oauth_scopes` row), `claimBuilder.ts` (pure §2.3 claim assembly, `daa_<ULID>` jti, rail-audience map), `schemas.ts` / `repo.ts` / `service.ts` (issuance + validation + revocation + list) / `routes.ts` (5 endpoints). `topics.ts` gains `AUTHORITY_ISSUED/REVOKED/EXPIRED`; `scopeCheck.ts` generalised to `requireScope(request, scope)`; deep-health gains `authorities` component; env extended (`IDENTITI_INTERNAL_SIGN_URL`, `IDENTITI_INTERNAL_HMAC_SECRET`, `HELPAN_INTERNAL_APP_ID`, `IDENTITI_TIMESTAMP_HEADER`, `JWT_DA_KID`). **Cross-rail:** `@kmv/platform-shared` idempotency plugin gained `exemptSuffixes` (rebuilt dist + 37/37 platform-shared tests still green). Test helpers: `testAuthorities.ts` (in-process signer with `forceError`), `testJwks.ts` gains `signStepUpToken`, `buildApp` gains `identitiKeyResolver` + `authoritySigner` overrides. Five new test files (scopeClassifier 10 + claimBuilder 6 unit; issuance 12 + validation 9 + revocation 5 real-DB). 42 new tests; total 153/153. No new deps (jose/kafkajs already present). No new migration. |
| H-3.1 | Migration `0009_business_op_id.sql` (adds `business_op_id` to `audit_log` + `actions`, partial index on `audit_log(business_op_id, created_at)`). `appendAuditEntry` + `AppendAuditEntryInput` extended with optional `agentId` / `delegatedAuthorityJti` / `targetRail` / `targetOperation` / `businessOpId`, all written to their own columns (INSERT widened). `entry_hash` composition unchanged (RECAP §6.13 / §6.18). Drizzle schemas `auditLog.ts` + `actions.ts` gain `businessOpId`. `authority.issue` / `authority.revoke` call sites repointed to pass `agentId` + `delegatedAuthorityJti` as structured fields (dropped from `detail`). Issuance + revocation integration tests assert the indexed columns. No new tests files; 153/153 still green. |
| H-3b | New `src/workers/cascadeRevocation/` — `cascade.ts` (`handleAccountEvent` core + `isTierDowngrade`; `ACCOUNT_SUSPENDED` → revoke all active, `TIER_CHANGED` downgrade → revoke high-stakes only; idempotent; publishes `AUTHORITY_REVOKED`) + `index.ts` (kafkajs consumer entrypoint, group `helpan.identiti.account.events.consumer`, SIGTERM drain). `authorities/repo.ts` gains `listActiveAuthoritiesForAccount`. `package.json` + `Procfile` gain the `cascade` process. New test file `cascadeRevocation.integration.test.ts` — 3 `isTierDowngrade` unit + 7 real-DB cascade checks. 10 new tests; total 163/163. No new deps. |
| H-8a | Rail-side dual-auth for the Console. `@kmv/platform-shared` `authPlugin` stands down when `request.appId` is already set by an earlier preHandler (re-vendored dist; 37/37 platform-shared tests green). `customerJwtPlugin` rewritten with `classifyPath` — `mandatory` (briefings, customer-JWT required) vs `optional` (the Console authority surface — Bearer verified, non-Bearer deferred to HMAC) vs `none`; `/validate` excluded. `app.ts` registers the customer-JWT plugin **before** the HMAC plugin. `authorities/routes.ts` gains `resolveCaller` — a customer caller skips the HMAC-scope check, is hard-scoped to its `account_uuid` (cross-customer read/revoke → 404), and grants/revokes audit as `helpan_console.*` / `actor_type='user'`; `authorities/service.ts` `AuthorityAuditContext` extended with `caller` + `callerAccountUuid`. New test file `authorities.console.integration.test.ts` — 10 real-DB checks. Total rail 173/173. No new deps, no new migration. Committed `b599242`. |
| H-8b (in flight) | New separate codebase `C:\Projects\helpan-console\` — `@kmv/helpan-console` RN library. Foundation shipped: `package.json` + `tsconfig` + `vitest.config`; `src/types.ts` (OpenAPI mirror); `src/api/client.ts` (`ConsoleApiClient` — 6 endpoints, envelope unwrap, `ConsoleApiError`); `src/i18n/` (EN + SW catalogue, `createTranslator`); `src/theme/tokens.ts` (`resolveTheme`); `src/format/` (`money` · `time` · `scopes`); `src/state/reducer.ts` (pure `consoleReducer` + selectors); `src/index.ts` public entry; `README.md` integration guide. 43/43 vitest tests, typecheck clean, no RN dependency in the foundation. Component layer + `HelpanConsole.open()` entry next. |
| H-8c | Migration `src/db/migrations/0010_newdocs_scopes.sql` — ALTER `oauth_scopes_rail_chk` to admit three new rails (`itafika`, `hakken`, `kipkiren_web_services`); INSERT 7 catalogue rows (Itafika `delivery.dispatch`; Hakken `discovery.query`; KWS Phase-2 paper-only `kws.{dns,ssl,mx,domain,uptime}.write`). New seed script `scripts/seedHelpanKwsAdmission.ts` + `npm run db:seed:helpan-kws` task that registers `helpan-kws-v1` in `agents` with stable id (NOT `agt_<ULID>` — deliberate exception so cross-rail audit references match the canonical KWS-side name), a Phase-1 safety policy (blacklists `write_money`), and an `agent.register` audit entry with §A.11 `agent_id` populated. One-off verifier `scripts/verifyH8c.ts` for post-seed inspection. RECAP §1 + §2 + §6 updated; no code change beyond seed scripts and the migration. |
| H-17 | New migration `src/db/migrations/0016_kafka_outbox.sql` — `kafka_outbox` table + partial pending-status index (same partial pattern as `actions_pending_idx`). New `src/db/schema/kafkaOutbox.ts` (Drizzle). New `src/lib/kafka/outbox.ts` — `enqueueOutboxEntry(tx, …)` for the producer side; `claimPendingBatch / markDelivered / markFailed` + the `drainOutboxOnce` orchestrator for the worker side; exponential backoff 1s→60s × 7 attempts before `abandoned`. New `src/workers/kafkaOutbox/index.ts` (5th Procfile worker). `Procfile` gains `outbox:` entry. `package.json` adds `worker:outbox` / `worker:outbox:start`. `.env.example` documents `KAFKA_OUTBOX_POLL_INTERVAL_MS` / `KAFKA_OUTBOX_BATCH_SIZE`. Producer migrations: `src/modules/events/service.ts` (BRIEFING_MATCHED), `src/modules/authorities/service.ts` (AUTHORITY_ISSUED + AUTHORITY_REVOKED), `src/modules/actions/service.ts` (ACTION_DISPATCHED + COMPLETED + FAILED, plus removed the now-dead `publishActionEvent` helper), `src/workers/cascadeRevocation/cascade.ts` (cascade-driven AUTHORITY_REVOKED) — every callsite moved from "after-commit publish" to "in-tx enqueueOutboxEntry". Test harness: `test/helpers/testDb.ts` gains `drainTestOutbox(handle, producer)` and the resetTestData truncates `kafka_outbox`. 5 existing integration tests updated to drain before reading `kafka.published`. New `test/integration/kafkaOutbox.integration.test.ts` — 7 real-DB cases (enqueue, deliver, retry-backoff, abandoned, ignore-future-attempts, batchSize bound, concurrent-drain disjoint). No new deps. |
| H-16 | New worker `src/workers/actionReaper/` — pure `reapStaleActions(deps, options)` in `reaper.ts` (one transaction per batch; per-row UPDATE + appendAuditEntry committed atomically) + Railway entrypoint `index.ts` with SIGTERM-aware loop. `src/modules/actions/repo.ts` gains `listStalePendingActions(tx, { olderThanCreatedAt, limit })` using raw `tx.execute(sql\`…\`)` with `FOR UPDATE SKIP LOCKED` (only place we hand-roll SQL — Drizzle's query builder doesn't expose lock hints). Manual snake_case→camelCase mapping with explicit Date normalisation since postgres-js returns ISO strings from raw queries (unlike Drizzle's typed select). `Procfile` gains a fourth process. `package.json` adds `worker:reaper` + `worker:reaper:start`. `.env.example` documents `REAPER_POLL_INTERVAL_MS` / `REAPER_STALE_AFTER_SECONDS` / `REAPER_BATCH_SIZE` — all optional, validated at worker boot via `envInt(name, default, min, max)`. New `test/integration/actionReaper.integration.test.ts` — 6 real-DB cases. No new deps. |
| H-15 | New migration `src/db/migrations/0015_audit_hash_version.sql` — adds `audit_log.hash_version smallint NOT NULL DEFAULT 1` with CHECK in (1, 2); idempotent backfill. `src/db/schema/auditLog.ts` gains the `hashVersion` field + CHECK. `src/lib/auditWriter.ts` adds `CURRENT_AUDIT_HASH_VERSION = 2`, `computeEntryHashV2`, `computeEntryHashForVersion`, and pins `appendAuditEntry` to v2 (inserts `hash_version = CURRENT_AUDIT_HASH_VERSION`). The v1 `computeEntryHash` is unchanged byte-for-byte — pre-H-15 rows verify forever. New `scripts/verifyAuditChain.ts` + `npm run audit:verify` task — walks chain in `(created_at, id)` order, dispatches to per-row hash composition, reports drift with row id + which check failed. `src/lib/auditWriter.test.ts` extended from 6 → 27 tests (v1 golden-hash regression guard + v2 determinism + v2 key-order invariance + 13 parameterised tamper-detection cases + v1/v2 dispatcher). No new deps. |
| H-12 | New migration `src/db/migrations/0014_family_discovery_scopes.sql` — two family_discovery-namespace catalogue rows. New `src/lib/matching/familyDiscoveryMatchers.ts` — two matchers + discriminators (`isFreshArrivalsBriefing`, `matchFreshArrivalsBriefing`, `isBasketRefillBriefing`, `matchBasketRefillBriefing`); the fresh_arrivals matcher re-imports `haversineKm` / `minutesInWindow` / `minutesOfDayInZone` / `parseHhmm` from `klokdShiftMatcher.ts` (single source of truth for the tz/geo helpers — no duplication). `DOMAIN_MATCHERS` table in `src/lib/matching/engine.ts` gains two entries (`family_discovery.fresh_arrivals`, `family_discovery.basket_auto_refill`). New `scripts/seedHelpanFamilyDiscoveryAdmission.ts` + `npm run db:seed:helpan-family-discovery` admitting `helpan-family-discovery-v1` with the §4.6 family_friendly safety policy (the only such audience in the portfolio). 15 new tests (`familyDiscoveryMatchers.test.ts` 13 unit, 2 engine routing). No new deps. |
| H-11 | New migration `src/db/migrations/0013_chapaa_scopes.sql` — four chapaa-namespace catalogue rows. New `src/lib/matching/chapaaMatchers.ts` — two matchers in one module (small, related): `isRoundUpBriefing` + `matchRoundUpBriefing` (KP `WALLET_DEBITED` consumer; configurable round-unit; computes deposit amount and exposes it in `detail.computed_round_up_minor`) and `isGoalAccelerationBriefing` + `matchGoalAccelerationBriefing` (`goal_id` identity + `alert_when` signal match). `DOMAIN_MATCHERS` table gains two entries. New `scripts/seedHelpanChapaaAdmission.ts` + `npm run db:seed:helpan-chapaa` admitting `helpan-chapaa-v1` with the §3.6 safety policy. 15 new tests (`chapaaMatchers.test.ts` 13 unit, two engine routing tests). No new deps. |
| H-10 | New migration `src/db/migrations/0012_lunchdrop_scopes.sql` — two lunchdrop-namespace catalogue rows (`lunchdrop.write.orders`, `lunchdrop.read.zone_feed`). New `src/lib/matching/lunchDropPlanMatcher.ts` — pure functions: `isLunchDropPlanBriefing` discriminator + `matchLunchDropPlanBriefing` (primary merchant first, fallbacks accepted, case-insensitive menu hit, per-order ceiling). `DOMAIN_MATCHERS` table in `src/lib/matching/engine.ts` gains one entry (`lunchdrop.weekly_plan`). New `scripts/seedHelpanLunchDropAdmission.ts` + `npm run db:seed:helpan-lunchdrop` task admitting `helpan-lunchdrop-v1` with the §2.6 safety policy (`category_whitelist=['food']`, `location_precision_floor=merchant_level`). 11 new tests (`lunchDropPlanMatcher.test.ts` 10 unit, one engine routing). No new deps. |
| H-9 | New migration `src/db/migrations/0011_klokd_scopes.sql` — three klokd-namespace catalogue rows (`klokd.write.shift_pay`, `klokd.write.shift_signup`, `klokd.read.worker_reputation`). New `src/lib/matching/klokdShiftMatcher.ts` — pure functions: `haversineKm`, `parseHhmm`, `minutesInWindow` (cross-midnight aware), `minutesOfDayInZone` (Intl.DateTimeFormat), `isKlokdShiftBriefing`, `matchKlokdShiftBriefing` (geo + time + category + pay). `src/lib/matching/engine.ts` refactored: `matchEventAgainstBriefings` now dispatches on `briefing.intent.domain` via a small `DOMAIN_MATCHERS` table (klokd registered there); unrecognised/missing domain falls back to the existing generic key-equality matcher. New `scripts/seedHelpanKlokdAdmission.ts` + `npm run db:seed:helpan-klokd` task that admits `helpan-klokd-v1` (stable name per `project-stable-agent-ids`) with the §1.6 safety policy. 20 new tests (`klokdShiftMatcher.test.ts` 17 unit, `engine.test.ts` 3 routing). No new deps. |
| H-4 | New `src/lib/periodWindow.ts` — `periodWindowKey(period, now)` lifted out of authorities/service.ts so the H-3 validator (read) and the H-4 dispatcher (write) agree on the bucket. `src/lib/kafka/topics.ts` gains `TOPIC_ACTION_EVENTS` + `EVENT_ACTION_DISPATCHED/COMPLETED/FAILED`. New `src/lib/dispatchers/` — `dispatcher.ts` (`TargetRailDispatcher` + `DispatchInput`/`DispatchOutcome` types + `createUnconfiguredDispatcher` sentinel), `httpDispatcher.ts` (production HTTP impl: rail-prefixed HMAC `Kipkiren-`/`Todoku-`/`Identiti-HMAC-SHA256`, attaches `X-Delegated-Authority` + `Traceparent` + `X-Business-Op-Id` + `X-Idempotency-Key`; AbortController-backed timeout; surface a stable `TARGET_RAIL_*` errorCode), `inMemoryDispatcher.ts` (test stub with programmable outcomes + call log), `registry.ts` (`buildProductionDispatchers(config)` selecting HTTP vs unconfigured per rail). New `src/modules/actions/` — `schemas.ts` (AJV: DispatchActionRequest with `account_uuid` / `target_rail` / `target_operation` / `payload` required, optional `initiated_by` / `amount_minor` / `business_op_id`; **agent_id pattern relaxed to `^[a-zA-Z0-9_-]{1,128}$`** to admit stable-name agents like `helpan-kws-v1` — Amendment §A candidate, see §6.20), `payloadRedactor.ts` (key-name + value-pattern PII redaction per Reboot Pack §9.5), `repo.ts` (insert / mark-completed / mark-failed / get / list with base64url cursor), `service.ts` (the three-phase orchestrator: validate → Phase A persist+audit+usage → outbound → Phase C terminal-status+audit; maps validator rejection reasons to HTTP error codes), `routes.ts` (POST dispatch HMAC-only scope `helpan:actions:dispatch`; GET dual-auth scope `helpan:actions:read`). `src/modules/authorities/repo.ts` gains `incrementAuthorityUsage(tx, {…})` — first writer to the `authority_usage` table the H-3 validator reads. `src/plugins/customerJwtPlugin.ts` `classifyPath` adds the `/v1/actions/*` cases. `src/app.ts` wires the actions module + dispatcher registry; `src/config/env.ts` gains `outboundDispatch` (per-rail baseUrl + dispatchPath + appId + hmacSecret + shared timestampHeader/timeoutMs). Test helpers `testApp.ts` adds `helpan:actions:dispatch` / `helpan:actions:read` scopes to the admin tenant + `outboundDispatch` defaults on `makeTestConfig`; `testDb.ts` accepts a `dispatchers` override + truncates `actions` in `resetTestData`. `vitest.config.ts` gains `testTimeout: 30000` (real-DB tests run 4 transactions × N dispatches per case). 19 new unit tests + 14 new real-DB integration tests (10 dispatch + 4 list). Total 206/206. No new migration. New deps: none. |

---

## 6. Open follow-ups (do not block H-4 / H-8 start)

### 6.1 Customer-JWT verifier — promote to platform-shared once a second caller appears
Shipped at H-2 in `src/plugins/customerJwtPlugin.ts` (decision 1a). Interface is generic by design — `keyResolver | KeyLike`, `issuer`, `audience`; decorates `request.customerJwt` + `request.appId` (from `X-App-Id`); error codes `AUTH_JWT_MISSING / _INVALID / _EXPIRED / _AUDIENCE`. Lift to `@kmv/platform-shared/fastify-customer-jwt` once KP-2 and TD-2 also need it; expected lift effort ~half-day.

### 6.2 Auth surface narrowed at H-2 — Amendment §A entry needed
Decision 2a: only `BearerCustomer` is accepted on `/v1/briefings/*` in v1.0. OpenAPI root lists three security alternatives; the rail enforces only one. **Action:** propose Amendment §A entry on the OpenAPI / Design Reference noting the v1.0 narrowing the next time a third-party agent flow asks for HMAC or service-JWT on briefings.

### 6.3 H-1 scope decisions surfaced to Chamia — still open
| Q | Topic | Default if no reply | Code impact |
|---|---|---|---|
| (a) | Deep-health `reason` field — embed in `components.<name>` object vs. sibling `components_detail` | a-i (amend OpenAPI to `{status, reason?}` object) | Small: schemas.ts + service.ts + OpenAPI yaml |
| (b) | pnpm workspaces + Turborepo migration (locked by Reboot Pack §131 + Instruction Pack §79; `kmv/platform-rails` monorepo per Reboot Pack §16.7) | b-iii (defer to KP-1 cross-rail sprint) | None at H-2 if deferred; large structural move if pulled forward |
| (c) | Ciphertext-from-day-one envelope (AES-256-GCM via new `@kmv/platform-shared/secrets` module — name avoids collision with response-envelope) | New module name `secrets`; blob format `secret:aes-gcm:v1:<key_id>:<iv_b64>:<ct_b64>:<tag_b64>` | Half-day on platform-shared + ~1h here to swap noop → real |

### 6.4 RLS write-policy + FORCE pattern — `delegated_authorities` reassessed at H-3
Migration 0006 shipped SELECT-only RLS for `briefings`, `delegated_authorities`, `actions`. H-2 closed the gap for `briefings` in 0007. **H-3 deliberately shipped no `delegated_authorities` migration** — on closer read the §6.4 user-scoped write-policy pattern does **not** apply: `delegated_authorities` is written by the issuance/revocation flow under HMAC/system context (a consuming-app server), with no `app.account_uuid` GUC set. The 0006 `authorities_user_read` SELECT policy stays dormant until H-8 wires customer-JWT Console reads; H-3's writes run as the (non-FORCEd) table owner. **When a non-superuser service role is introduced** (post-Stage-1 sandbox), `delegated_authorities` will need INSERT/UPDATE policies keyed on a system/app context — not the account-UUID pattern. Track as a deployment-hardening item. `actions` (H-4) is the same shape — system-mutated, not user-mutated; the §6.4 account-UUID pattern is for `briefings` only.
The fixed account-scoped pattern (still correct for any future user-mutated table):
```sql
CREATE POLICY <name>_user_insert ON <table> FOR INSERT
  WITH CHECK (account_uuid = current_setting('app.account_uuid', true));
CREATE POLICY <name>_user_update ON <table> FOR UPDATE
  USING      (account_uuid = current_setting('app.account_uuid', true))
  WITH CHECK (account_uuid = current_setting('app.account_uuid', true));
CREATE POLICY <name>_user_delete ON <table> FOR DELETE
  USING      (account_uuid = current_setting('app.account_uuid', true));
ALTER TABLE <table> FORCE ROW LEVEL SECURITY;
```
Without `FORCE`, a connection running as the table owner silently bypasses every policy.

### 6.5 Audit hash-chain writer — direct reuse at H-4
`src/lib/auditWriter.ts` exposes pure `computeEntryHash(...)` (unit-tested, key-order-stable canonical JSON) and `appendAuditEntry(tx, ...)`. Concurrency is serialised by `pg_advisory_xact_lock(7268010825743210)` inside the transaction — constant across all callers. H-4 should call `appendAuditEntry` for action-dispatch entries with `actorType: 'agent'` and the `delegated_authority_jti` populated; no changes to the writer expected.

### 6.6 Type-aware matchers per `briefing_type` — ✅ CLOSED by H-9 (21 May 2026)
H-5 plan decision 2b shipped the generic key-equality matcher only. H-9 refactored `src/lib/matching/engine.ts` to a strategy router that dispatches on `briefing.intent.domain` with a `DOMAIN_MATCHERS` table — the Klokd `klokd.shift_search` matcher is the first entry. Future per-app sprints (H-10 Lunch Drop, H-11 Chapaa, H-12 family-discovery) register their matcher by adding one line to the table; the engine's external API is unchanged.

### 6.7 Kafka outbox — ✅ CLOSED by H-17 (22 May 2026)
Migration 0016 adds `kafka_outbox` (id, topic, partition_key, payload, headers, status, attempts, next_attempt_at, delivered_at, last_error, created_at) with a partial `(next_attempt_at, created_at) WHERE status='pending'` index. New `src/lib/kafka/outbox.ts` exports `enqueueOutboxEntry(tx, …)` for producers + `drainOutboxOnce(db, producer, opts)` for the worker. Producers in events / authorities / actions / cascade all migrated — they now INSERT inside the business tx so emission is atomic with the write. New `src/workers/kafkaOutbox/` (fifth Procfile process) polls with `FOR UPDATE SKIP LOCKED` (multi-replica safe). Exponential backoff 1s/2s/4s/8s/16s/32s/60s → abandoned. Empty `KAFKA_BROKERS` → drainer sleeps; rows accumulate (at-least-once-when-wired semantics). Test harness gained `drainTestOutbox(handle, producer)` to synchronously drain the in-memory producer so existing `kafka.published` assertions keep working AND prove the outbox pipeline end-to-end. 7 new integration tests + 5 existing producer tests updated. Closes RECAP §6.7.

### 6.8 Webhook URL discovery is env-driven at H-5 — registry table at v1.1
`HELPAN_WEBHOOK_URL_<APP_ID_UPPER>` per app. Works for the four v1 consuming apps. Replace with `app_webhook_targets` table when (a) the consuming-app onboarding flow exists (H-7 operator console adjacent) or (b) any app needs multiple webhook endpoints / per-event-type routing.

### 6.10 Admin-scope enforcement is rail-local at H-6 — promote to platform-shared on second caller
`src/lib/scopeCheck.ts` provides `requireAdminScope(request)` reading `request.tenantRecord.scopes`. Promote to `@kmv/platform-shared/scope-check` once KP/Todoku also need scoped admin endpoints. Interface is one function + one error class — verbatim lift.

### 6.11 OAuth scope catalogue listing is HMAC-authed at H-6 — Amendment §A candidate
OpenAPI says GET /v1/oauth/scopes is `security: []` (public). H-6 ships it HMAC-authed because the shared HMAC plugin's exemption list is path-based, not method-aware (POST on the same path requires HMAC). v1.1 options: (i) extend the shared plugin to support method-aware exemptions; (ii) keep HMAC-on-listing and document the v1.0 narrowing in Amendment §A. Decide when a third-party agent onboarding tool actually needs anonymous discovery.

### 6.12 Safety policy "create" path piggybacks on PUT — POST endpoint deferred to H-7+
OpenAPI doesn't define POST /v1/operator/safety-policies. H-6 uses RFC-compliant PUT-as-upsert (unknown policy_id → 201 create, known → 200 replace), but operators have to invent the ULID locally. Either (a) add a server-side `POST /v1/operator/safety-policies` that mints the id, or (b) leave PUT-only and surface the convention in operator-console UX. Likely lands with H-7's operator-console hardening.

### 6.13 Reboot Pack v1.2 + Amendment §A — platform commitments locked H-3 / H-4 must implement
Reconciled 13 May 2026. The Reboot Pack v1.2 (4 May 2026 consolidation) + Amendment §A (7 May 2026 cross-rail scan integration) lifts five cross-rail rules into the platform record that affect Helpan AI directly:

| Ref | Rule | Helpan-AI impact | Sprint |
|---|---|---|---|
| §A.5 | Helpan AI is the agent-runtime rail; supersedes scan ID-2. **Not** a fourth rail in the §3 regulated-rails table. Identiti = OAuth issuance authority; Helpan AI = per-call validation, scope catalogue, agent registry, delegated-authority registry, action dispatch, agent audit log. | Standing position updated in §1 above. No code impact — clarifies positioning for external comms / docs / Amendment §A on Helpan AI Design Reference. | — |
| §A.2 (XR-1) | When `actor.type='agent'`, relying rails (KP, Todoku) MUST call `POST /authorities/{id}/validate` **per call** before accepting the operation. `actor` and `initiated_by` are optional JWT claims propagated end-to-end. | H-3 ships `/validate` as the most-called endpoint of the rail. Contract is now platform-locked (not just Helpan-internal). | **H-3** |
| §A.11 | Cross-rail audit invariant: identical `actor` + `initiated_by` across Identiti / KP / Todoku audit log entries for a single business operation, keyed off shared `traceparent` + `business_op_id`. **Hard build-acceptance criterion** for any rail consuming step-up tokens. | H-4 dispatch and H-2 audit-log writer must persist `actor`, `initiated_by`, `traceparent`, `business_op_id`. H-2 schema already has the columns (verify); H-4 propagation is the new work. | **H-4** (+ verify H-2) |
| §A.1 | JIT identity posture is platform-wide: no long-lived bearer tokens, no static service accounts, no refresh on elevated scopes. TTL table at §16.8 (step-up 5 min single-use; phone token 15 min audience-bound). | Helpan-AI-issued delegated-authority JWTs must follow the same JIT semantics (short-TTL, audience-bound, single-operation where applicable). H-3 token-issuance design choice. | **H-3** |
| §A.9 | Kafka topology **MUST not preclude** per-token revocation events. CAEP real-time revocation is v1.1 roadmap. | `helpan.authority.events` topic schema must include an event-type that maps to per-token revocation (or be extensible to one) — even if H-3 only emits issuance / scope-change / explicit-revocation at v1.0. | **H-3** |

Action items into H-3 design:
1. `/validate` endpoint shape and error codes must match the contract relying rails will call per Reboot Pack §A.2 — coordinate the wire format with Identiti ID-10 (signing API) and the KP/Todoku validator callsites before locking.
2. `helpan.authority.events` topic catalogue must include `AUTHORITY_REVOKED` event-type at v1.0 with a schema extensible to CAEP-style push-revocation at v1.1.
3. Delegated-authority JWT TTL: align with the §16.8 table — step-up class (5 min single-use) for one-shot actions; phone-token class (15 min audience-bound) for short-burst flows. Long-lived agent credentials are out.

**Audit-invariant gap (verified 13 May 2026 against `src/db/schema/auditLog.ts`, `src/db/schema/actions.ts`, `src/lib/auditWriter.ts`) — must be closed before H-4 ships:**

| Field | `audit_log` column | `appendAuditEntry` writer | `actions` column | Status |
|---|---|---|---|---|
| `traceparent` | ✓ present | ✓ exposed + inserted | ✓ present | OK |
| `actor.type` → `actor_type` | ✓ present | ✓ exposed + inserted | ✓ present | OK |
| `actor.agent_id` → `agent_id` | ✓ present | ❌ writer does NOT accept it; column is NULL on every existing audit row | ✓ present | **Gap — writer extension** |
| `actor.delegated_authority_jti` → `delegated_authority_jti` | ✓ present | ❌ writer does NOT accept it; column is NULL on every existing audit row | ✓ present | **Gap — writer extension** |
| `initiated_by` | ✓ present | ✓ exposed + inserted | ✓ present | OK |
| `business_op_id` | ❌ **column does not exist** | ❌ not exposed | ❌ does not exist (only `app_correlation_id`, which is app-side not cross-rail) | **Gap — migration + writer + schema** |
| `target_rail` / `target_operation` | ✓ present | ❌ writer does NOT accept either | ✓ present | **Gap — writer extension** (needed by H-4 dispatch entries) |

Required work, ordered:
1. **Migration 0009** — add `business_op_id text` to `audit_log` and `actions` (nullable; index on `(business_op_id, created_at)` for cross-rail forensic reconstruction). Coordinate the exact column name with KP and Todoku before applying — Reboot Pack §A.11 says "shared `business_op_id`" so the three rails must use the same identifier name on the wire and in storage.
2. **`appendAuditEntry` extension** — add optional `agentId`, `delegatedAuthorityJti`, `targetRail`, `targetOperation`, `businessOpId` to `AppendAuditEntryInput`; include them in the INSERT. Backward-compatible (all optional). Hash-chain input unaffected (currently just `id|actor_id|action|resource_id|detail|previous_hash`) — decide whether the new fields should also be folded into `entry_hash` to keep them tamper-evident. Likely yes; that means a chain reset is not required but the hash composition changes from a given migration date forward — document the cutover.
3. **Backfill audit of existing call sites** — `event.ingested`, `oauth_scope.create`, `agent.register`, `agent.status_change`, `safety_policy.create`, `safety_policy.update`, `briefings.*`. Pass `agentId` / `delegatedAuthorityJti` where the operation is agent-initiated; otherwise leave NULL. Existing rows stay NULL by design.
4. **H-4 dispatch must populate all of the above on every audit entry + every `actions` row.** Per §A.11, mismatched values across Helpan/KP/Todoku for the same `business_op_id` is a build-acceptance failure.

This is closer to a half-sprint of incremental work than a verification step. Recommend folding it into H-3 (since H-3 already touches the audit-log writer for issuance/validation/revocation entries) rather than waiting for H-4.

**✅ CLOSED by H-3.1 (18 May 2026).** Migration `0009_business_op_id.sql` adds `business_op_id` to `audit_log` + `actions` (the other §A.11 columns — `agent_id`, `delegated_authority_jti`, `target_rail`, `target_operation` — already existed). `appendAuditEntry` gained optional `agentId` / `delegatedAuthorityJti` / `targetRail` / `targetOperation` / `businessOpId`, all written to their own columns. `authority.issue` / `authority.revoke` now populate `agent_id` + `delegated_authority_jti` in the indexed columns. **Hash-composition decision:** the new columns are NOT folded into `entry_hash` — consistent with the existing partial-coverage design (`actor_type`, `outcome`, `account_uuid`, `initiated_by` were already unhashed). No `hash_version`, no chain reset, genesis→head stays uniform. A future full-column-hash hardening (covering all currently-unhashed columns, with a `hash_version` discriminator) is a separate item — see §6.18. H-4 dispatch still owns populating `target_rail` / `target_operation` / `business_op_id` on its action entries.

### 6.14 Cascade-revocation Kafka consumer — ✅ CLOSED by H-3b (18 May 2026)
Shipped `src/workers/cascadeRevocation/` — `cascade.ts` (`handleAccountEvent` core: `ACCOUNT_SUSPENDED` → revoke all active authorities for the account; `TIER_CHANGED` downgrade → revoke high-stakes only) + `index.ts` (kafkajs consumer on `identiti.account.events`, group `helpan.identiti.account.events.consumer`, SIGTERM-aware). Idempotent under at-least-once redelivery. Publishes `AUTHORITY_REVOKED` per revoked authority. 10 new tests. `ACCOUNT_DELETED` / `CONSENT_REVOKED` have no v1.0 Identiti source (H4 §5) — v1.1.

### 6.18 Audit chain full-column hash — ✅ CLOSED by H-15 (21 May 2026)
Pre-H-15 `entry_hash` covered `id | actor_id | action | resource_id | detail | previous_hash` only. H-15 ships migration 0015 (adds `hash_version smallint NOT NULL DEFAULT 1` with a CHECK on the allowed versions), extends `auditWriter.ts` with a v2 composition that folds in every previously-unhashed column (`actor_type | account_uuid | app_id | outcome | initiated_by | traceparent | agent_id | delegated_authority_jti | target_rail | target_operation | business_op_id | resource_type | request_id`), and pins the writer to `CURRENT_AUDIT_HASH_VERSION=2`. The v2 hash starts with the literal `"v2"` prefix so a v2 row cannot be misinterpreted as v1 even if its `hash_version` column is tampered. Existing rows (genesis + 5 admission entries) stay at v1 and verify against the legacy composition; the chain pointer (`previous_hash`) is composition-agnostic so the v1→v2 cutover is seamless. New `scripts/verifyAuditChain.ts` + `npm run audit:verify` walks the chain row-by-row, dispatching to `computeEntryHashForVersion` per row, reports any drift. Live state verified intact (6 rows: 5 v1 + 1 genesis). Bumping to v3 in future is a one-line change in `CURRENT_AUDIT_HASH_VERSION` + a new composition function + a CHECK constraint relax; the v1 and v2 functions stay verbatim.

### 6.15 Authority `scope_covers` is exact-match in v1.0 — operation→scope resolver is v1.1
`POST /validate` computes `scope_covers` by exact equality of `intended_operation` against an authority `scope_id`. The Delegated Authority Contract §4.4 *example* shows different strings (`intended_operation: kipkiren_pay.payment.execute` vs `scope_id: kipkiren.write.payments`) but never specifies the mapping algorithm. v1.0 expects the relying party to pass the `scope_id` it intends to exercise as `intended_operation`. A richer operation→scope catalogue resolver is a v1.1 item — flag an Amendment §A entry on the Delegated Authority Contract / OpenAPI when KP/Todoku validator callsites are wired.

### 6.16 Expired-authority revoke — contract §7.3 is self-contradictory (Amendment §A candidate)
Delegated Authority Contract §7.3 table says `AUTHORITY_EXPIRED | 410` but the same row's prose says "still 200 (idempotency)". OpenAPI groups "already revoked or expired" into **409**. H-3 follows OpenAPI: expired revoke → 409 `AUTHORITY_EXPIRED`. **Action:** propose an Amendment §A entry reconciling §7.3 to 409.

### 6.17 `exemptSuffixes` added to `@kmv/platform-shared` idempotency plugin (H-3 decision 3a)
Done — `IdempotencyPluginConfig.exemptSuffixes` mirrors `exemptPaths` / `exemptPrefixes`, matched with `path.endsWith()`. Used by H-3 to exempt `POST /v1/authorities/{id}/validate` (a pure query — a cached validate result would break the §4.6 cache rules). dist rebuilt; 37/37 platform-shared tests still green. KP/Todoku can use it for any POST-query endpoint under a variable path segment.

### 6.20 OpenAPI `Action.agent_id` pattern conflicts with stable-name agents (Amendment §A candidate)
The OpenAPI spec at `Action.agent_id` pins `^agt_[0-9A-HJKMNP-TV-Z]{26}$` — only mint-fresh `agt_<ULID>` shapes. H-8c admitted `helpan-kws-v1` as a stable cross-rail name per memory `project-stable-agent-ids` and the same exception is expected for any future cross-rail-named agent (LipaStack per-merchant, Itafika dispatch, future internal automations). The rail-local `src/modules/actions/schemas.ts` AGENT_ID_PATTERN was relaxed to `^[a-zA-Z0-9_-]{1,128}$` so both shapes validate. **Action:** propose an Amendment §A entry on the OpenAPI spec broadening the pattern — same treatment that landed for the operator-agents and authorities surfaces would be a single coordinated change.

### 6.21 H-4 dispatch is synchronous-only — async/202 + outbox patterns deferred (**reaper subsystem CLOSED by H-16**)
Pending-action reaper landed at H-16 (22 May 2026) — the fourth Procfile process (`reaper`) settles `actions.status='pending'` rows older than `REAPER_STALE_AFTER_SECONDS` (default 600s) to `failed/REAPER_UNRESOLVED` with an `action.fail` audit entry chained off the previous head. Closes the operational gap where a Phase-A-committed-Phase-B-crashed action would sit in pending indefinitely. The async/202 + outbox transport patterns themselves remain v1.1 candidates, but the immediate v1.0 crash-window concern is settled.

### 6.21 (legacy — pre-H-16 framing kept for context)
The OpenAPI spec admits both 200 (synchronous completed) and 202 (accepted; result via webhook + `GET /actions/{id}`). H-4 ships 200 only: the dispatcher's outbound call is awaited in-line and the action transitions to its terminal state before the response. **Crash window:** if the rail dies between Phase A commit and Phase B's outbound call, the action sits in `pending` indefinitely. The cross-rail audit chain is intact (Phase A wrote `action.dispatch`), but an operator needs to reconcile. v1.1 candidates: (a) a background reaper that ages `pending` actions and re-attempts or fails them; (b) an outbox table + drainer process consistent with §6.7's authority-events outbox; (c) async/202 + relying-party callback. Defer until either KP or Todoku produces measurable target-rail latency ≥1s on the live wire (v1.0 wire-up uses in-memory dispatchers via integration tests; HTTP dispatcher is wired but URLs are empty by default).

### 6.22 `authority_usage` write happens on every attempt, not only on success (§A.1 semantics)
H-4 increments `cumulative_minor` + `call_count` in Phase A, before the outbound dispatch runs. A failed dispatch (e.g. `TARGET_RAIL_REJECTED`) still consumes the per-period window. This matches §A.1 JIT semantics — *a single-operation token is single-operation regardless of outcome* — but means a relying party's downstream rejection still subtracts from the per-period budget. The alternative (rollback on failure) would let a misbehaving relying party repeatedly burn through validation without spending budget. Keep the current behaviour; document it on the Delegated Authority Contract §4 when an Amendment §A pass is opened.

### 6.19 Helpan Console UI layer — deferred from H-8b close (21 May 2026)
H-8 closed with the rail-side dual-auth (H-8a) and the RN library **foundation** (H-8b). The Console UI layer — `AuthorityCard`, `ScopeList`, `RevokeConfirmModal`, `BehaviouralFrictionScreen`, `EmptyState`, `OfflineBanner`, Tabs, the Active / Activity / History screens, `AuthorityDetail`, `GrantFlow`, the `useConsoleData` hook, the imperative `HelpanConsole.open()` entry, Storybook stories, and the WCAG 2.1 AA pass — does **not** ship at H-8. Reactivate as a future Console sprint when: (a) at least one embedding app (Lunch Drop / Chapaa / Klokd) commits to wiring the Console into its mobile build, AND (b) H-4 lands so the Activity tab is non-empty, AND (c) Identiti seeds Helpan AI's internal-signing tenant credential so the Grant flow has live issuance to talk to. Until then the foundation sits at `C:\Projects\helpan-console\` ready to extend; the `ConsoleApiClient` already binds to the live H-8a dual-auth surface so the contract is locked. **Pre-requisite when work resumes:** init `helpan-console` as its own GitHub repo (parallel to `whyyam1/helpan_ai`).

### 6.9 Other observations worth carrying forward
- **Vitest worker race on migrations** — two parallel test files calling `migrate(...)` collide on `_drizzle_migrations` PK or `ALTER TABLE` schema mutations. H-5 resolved with `pg_advisory_lock(7268010825743211)` in `withRealDb`. KP/Todoku integration test setups will need the same pattern.
- **Vitest file parallelism races on a shared DB.** H-6 grew the integration suite enough that file A's `TRUNCATE` mid-run wiped file B's seeded data. Fixed with `fileParallelism: false` in `vitest.config.ts` — single process, files serialised. Slower (~96s with real DB vs ~25s parallel) but correct. KP/Todoku will hit the same.
- **AJV `coerceTypes:false` is rail-wide.** Query-string ints don't coerce; H-2 used "string-with-numeric-pattern + handler-side `parseInt`" for `limit`. KP-2/TD-2 will hit the same on any GET with a numeric query param.
- **postgres-js Date binding via tagged templates** — at H-5 we observed Bind-step errors when a `Date` was interpolated directly. Workaround: pass `.toISOString()::timestamptz`. Adopted across the worker + tests.
- **Identiti closed ID-1..ID-10** (8–15 May 2026; 147/147 tests). H-2 unblocked by ID-3; H-3 unblocked + closed by ID-10 (`POST /v1/internal/sign` live).
- **Todoku consolidated** `c:\Projects\todoku\` → `c:\Projects\todoku-prod\`. Stale path references in this folder fixed in `src/db/client.ts` and `helpan-ai-scan-integration-memo-v1.md` during H-1.
- **`prepare:false` postgres-js pin** is now rail-wide per Todoku TD-0; H-1 matched. Identiti and KP should adopt the same.

---

## 7. Recently closed

| Item | Closed at | Notes |
|---|---|---|
| Real-Postgres smoke (was H-1 §6.1) | H-2 (9 May 2026) | Full suite ran against `helpan_ai_test` on `localhost:5432` (`postgres:postgres`). 7 migrations applied cleanly via drizzle-kit's runtime migrator. 12/12 real-DB tests green. Supabase af-south-1 provisioning for staging still pending Ops. |
| Kafka first use (was H-1 stack note "first use at H-3") | H-5 (11 May 2026) | H-5 introduced the producer surface — `kafkajs` in prod, in-memory in tests. `helpan.briefing.events` topic publishing `BRIEFING_MATCHED`. H-3 will reuse the same surface for `helpan.authority.events`; H-4 for `helpan.action.events`. |
| H-3 hard blocker — Identiti ID-10 (H4 joint contract) | H-3 (18 May 2026) | ID-10 closed 15 May 2026 — `POST /v1/internal/sign` + `helpan_authority_issuance` step-up audience + `helpan-da-*` kid live. H-3 built against it with an in-process signer stub in tests; production wires `createHttpIdentitiSigner`. |
| Real-Postgres **staging** smoke (Supabase) | 18 May 2026 | `.env` wired to the Supabase project `oklovgfjrnsfxrlcfapg` (eu-west-1, Postgres 17.6); migrations 0001–0009 applied; 14 rail tables + audit genesis + 11 seeded scopes verified. The dev-only `localhost` smoke (H-2) is now backed by a real staging DB. (Superseded 20 May 2026 — see "Supabase project re-pointed" below.) |
| §6.13 audit-writer extension (was "top H-4-prep item") | H-3.1 (18 May 2026) | Migration 0009 + `appendAuditEntry` §A.11 fields. See §6.13. |
| §6.14 cascade-revocation Kafka consumer | H-3b (18 May 2026) | `src/workers/cascadeRevocation/`. See §6.14. |
| §6.1 customer-JWT verifier — promotion deferral re-evaluated | H-8a (19 May 2026) | Still rail-local (`customerJwtPlugin`), not yet lifted to platform-shared — KP-2/TD-2 haven't started so the "second caller" trigger isn't met. H-8a grew the plugin (`classifyPath`, deferrable mode) but it stays rail-local; lift when KP/Todoku need it. |
| §6.2 customer-JWT on the authority surface (H-3 decision 1a deferral) | H-8a (19 May 2026) | `/v1/authorities` issue/list/detail/revoke now dual-auth (customer-JWT or HMAC). The H-3 §6.2 note "customer-JWT Console list/revoke deferred to H-8" is discharged. |
| GitHub repo provisioned | 19 May 2026 | `github.com/whyyam1/helpan_ai`. Three commits pushed; `Co-Authored-By: Claude …` trailer rewritten out of all commit messages with `git filter-branch --msg-filter` + force-pushed per user direction. Memory entry `feedback-no-claude-coauthor` captures the rule for future commits in KMV repos. |
| Supabase project re-pointed | 20 May 2026 | Old project `oklovgfjrnsfxrlcfapg` dropped; `.env DATABASE_URL` now points at `jvkhoveeayixbjnhmqxa` (eu-west-1, session pooler `aws-0-eu-west-1.pooler.supabase.com:5432`). Migrations need re-running against the new project — open work item, see §1. |
| Railway `web` service live | 20 May 2026 | Railpack v0.23.0 build (`npm ci` 247 packages → `tsc -p tsconfig.json`) succeeds; container boots and binds Railway-injected `PORT`. First two deploys crash-looped with `Missing required environment variable: AUTH_RAIL_PREFIX` — `.env` is gitignored, so no config reached Railway until the 12 service variables (`NODE_ENV=production`, `LOG_LEVEL=info`, `SERVICE_VERSION=0.1.0`, `DATABASE_URL`, `AUTH_RAIL_PREFIX=Helpan`, `AUTH_TIMESTAMP_HEADER=x-helpan-timestamp`, `AUTH_TIMESTAMP_TOLERANCE_SECONDS=300`, `IDEMPOTENCY_TTL_SECONDS=86400`, `SECRETS_ENVELOPE_PROVIDER=noop`, `IDENTITI_JWKS_URL`, `IDENTITI_JWT_ISSUER`, `HELPAN_JWT_AUDIENCE`) were pasted into the Raw Editor. `worker` + `cascade` Procfile entries not yet provisioned. |
| **H-8 closed (scope-trimmed)** | 21 May 2026 | Rail-side (H-8a) + RN library foundation (H-8b) shipped; Console **UI layer deferred** to a future sprint — see §6.19. Reactivates when an embedding app schedules integration, H-4 lands, and Identiti seeds Helpan AI's internal-signing tenant credential. |
| All five `app_credentials` tenants seeded | 22 May 2026 | `lunchdrop` re-seeded with the coherent full scope set (admin + consuming-app); `klokd`, `chapaa`, `family_discovery` seeded as internal consuming-app tenants with `authorities:issue/validate/revoke` + `actions:dispatch/read`; `helpan_ai` seeded as the internal/operator tenant (`helpan:admin, operator:read`). HMAC secrets generated per tenant, wrapped via the noop envelope at rest, plaintext handed off out-of-band. Re-running `npm run db:seed:app -- <app_id> ...` rotates. New `scripts/verifyTenants.ts` (read-only) prints the row set for ops checks. |
| **H-17 closed — Kafka outbox (§6.7 close)** | 22 May 2026 | Migration 0016 adds `kafka_outbox`. Five producer call sites moved from after-commit publish to in-tx enqueue. New `kafkaOutbox` worker drains via `FOR UPDATE SKIP LOCKED` and publishes to Kafka with exponential backoff. Drained-test-outbox harness keeps existing producer tests passing AND proves the pipeline end-to-end. 7 new + 5 updated tests; 301/301 local. Supabase migration pending pooler retry (transient ENOTFOUND at deploy time). |
| **H-16 closed — pending-action reaper (§6.21 close)** | 22 May 2026 | Fourth Procfile worker `reaper`. Polls `actions.status='pending'` for rows older than `REAPER_STALE_AFTER_SECONDS` (default 600s) using `FOR UPDATE SKIP LOCKED` and settles to `failed/REAPER_UNRESOLVED` with an `action.fail` audit entry chained. Closes the H-4 Phase-A-commit-Phase-B-crash window. Deliberately does NOT retry (the original payload is lost on crash; the redacted payload would corrupt downstream state). 6 new real-DB tests; rail 294/294. Supabase chain verified intact post-deploy. |
| **H-15 closed — audit-chain full-column hash (§6.18)** | 21 May 2026 | Stage-2 pen-test prep. Migration 0015 adds `hash_version` to `audit_log` with v1 backfill + v2 cutover. v2 composition covers every previously-unhashed column (eleven of them) + a `"v2"` literal prefix so a row can't be re-interpreted as v1 via hash_version tampering. New verifier walks chain dispatching by per-row version. Live state confirmed CHAIN INTACT. 21 new unit tests including a v1 golden-hash regression guard. Closes RECAP §6.18. |
| **H-12 closed — family-discovery per-app; PER-APP BLOCK COMPLETE** | 21 May 2026 | Last per-app sprint. Migration 0014 adds two `family_discovery.*` scopes. Two new matchers wired via `DOMAIN_MATCHERS` (fresh_arrivals + basket_auto_refill); fresh_arrivals re-uses Klokd's pure tz/geo helpers — single source of truth. `helpan-family-discovery-v1` agent admitted with the only `family_friendly` audience_posture in the portfolio. 15 new tests. Standing-basket cron-firing is v1.1 (gated on KP C.3). Brand-lock TBD per §4.8 — required before Stage 2. **All four per-app sprints closed**; remaining buildable rail work is gated (H-13 / H-14 / H-8d / Console UI). |
| **H-11 closed — Helpan Chapaa per-app** | 21 May 2026 | Third per-app sprint, highest-stakes integration in the portfolio. Migration 0013 adds four `chapaa.*` scopes (including the `read_behavioural` Console-friction-screen scope). Two new matchers wired via `DOMAIN_MATCHERS`: round-up (KP wallet-debit consumer with configurable round-unit math) and goal-acceleration (`goal_id` + signal match). `helpan-chapaa-v1` agent admitted with §3.6 safety policy. 15 new tests. MMF rebalance stays suggest-only at v1.0; credit-unlock partner-lender is KP-side. Unblocks H-12 (family-discovery, last per-app). |
| **H-10 closed — Helpan Lunch Drop per-app** | 21 May 2026 | Second per-app sprint. Migration 0012 adds two `lunchdrop.*` catalogue scopes. New `lunchDropPlanMatcher` registered in the `DOMAIN_MATCHERS` table — matches `lunchdrop.weekly_plan` briefings on merchant + menu_preference + per-order ceiling. `helpan-lunchdrop-v1` agent admitted with §2.6 safety policy (`category_whitelist=['food']`). 11 new tests. Cron-driven scheduled-action firing deferred to v1.1 with programmable money (KP Gap §C.3). |
| **H-9 closed — Helpan Klokd per-app** | 21 May 2026 | First per-app sprint. Migration 0011 adds three `klokd.*` catalogue scopes. New `klokdShiftMatcher` (geo + tz-aware time-window + category whitelist + pay floor) wired into the matching engine via a strategy table by `intent.domain`; closes §6.6 (type-aware matchers deferred to per-app sprints). `helpan-klokd-v1` agent + §1.6 safety policy admitted via new seed script. 20 new tests. Pay-on-completion + reputation-surfacing live flows run on the embedding Klokd backend per Per-App Patterns §1.5; rail-side ships the catalogue + agent + matcher. Unblocks H-10 (Lunch Drop), H-11 (Chapaa), H-12 (family-discovery). |
| **H-4 closed** | 21 May 2026 | Agent action dispatch — `POST /v1/actions/dispatch` + `GET /v1/actions[/:id]`. §A.11 audit invariant enforced on every call (two-phase persistence; both audit rows carry `agent_id` / `delegated_authority_jti` / `target_rail` / `target_operation` / `business_op_id` / `traceparent`). `authority_usage` finally written (the H-3 validator has been reading from an empty table). `TargetRailDispatcher` abstraction with HTTP + in-memory + unconfigured-sentinel impls; URLs / secrets independently opt-in per rail. New Kafka topic `helpan.action.events`. PII redactor strips persisted-side payload values. 33 new tests; 206/206 green. Unblocks H-9 (Klokd) + per-app sprints. Carries forward: §6.20 (OpenAPI agent_id pattern), §6.21 (async/outbox deferred), §6.22 (usage write on every attempt). |
| Migrations 0001–0009 re-applied to new Supabase | 21 May 2026 | `npm run db:migrate` against `jvkhoveeayixbjnhmqxa`. All 9 migrations recorded in `_drizzle_migrations`; pgcrypto + RLS in place. |
| First `app_credentials` tenant seeded | 21 May 2026 | New `scripts/seedAppCredential.ts` + `npm run db:seed:app` task. Lunch Drop registered as `lunchdrop` / internal with scopes `[helpan:admin, helpan.read.briefings, helpan.write.briefings, helpan.read.activity, helpan.admin.authorities]`. HMAC secret generated server-side (32 random bytes hex), wrapped via the noop envelope, handed off out-of-band to the embedding app. Re-running the script rotates the secret. |
| **H-8c closed — `helpan-kws-v1` admitted + newdocs scope catalogue additions** | 21 May 2026 | Migration 0010 + `scripts/seedHelpanKwsAdmission.ts`. Closes McKinsey P1 + Reboot Pack §14 open CEO action. Three new rails admitted to the catalogue check constraint (`itafika`, `hakken`, `kipkiren_web_services`); seven new scope rows; `helpan-kws-v1` registered in `agents` with stable name (not `agt_<ULID>`); Phase-1 safety policy blocks `write_money`; admission entry on the audit chain. Open questions Q3 / Q4 / Q5 / Q7 from the newdocs pack deferred; this commit takes the JIT-compliant side on KWS TTL pending joint Identiti decision. |

---

*Helpan AI Rail · Build Progress · 7 May 2026 · last updated 21 May 2026 · update at each sprint close*
